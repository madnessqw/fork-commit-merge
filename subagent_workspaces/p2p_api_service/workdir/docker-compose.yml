version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./p2p_api.db
      - ENVIRONMENT=production
    volumes:
      - ./data:/app/data
