"""UniverseCreator automation scripts package.

This file makes the local `scripts/` directory a real Python package so
imports like `from scripts import update_summary` resolve to the repo copy
instead of drifting into another namespace-package path on the machine.
"""

